import { Stack, Text } from '@mantine/core';
import type { GetArticleCardDTO } from 'smth-shared/src/dto/article.dto.ts'

const SmallArticleCard: React.FC<GetArticleCardDTO> = (props) => {
    return (
        <Stack p={20}>
            
        </Stack>
    )
}

export default SmallArticleCard;